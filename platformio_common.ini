[nano]
port = /dev/cu.wchusbserial*

[env]
framework = arduino
monitor_speed = 115200
monitor_flags =
    --parity
    N
    --eol
    LF
    --encoding
    UTF-8


[env:nano]
platform = atmelavr
board = nanoatmega328
monitor_port = ${nano.port}
upload_port = ${nano.port}
